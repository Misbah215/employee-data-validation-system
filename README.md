# employee-data-validation-system
A database-driven automation system that validates employee data, logs errors into a database, and processes only valid records to generate reports and send emails. The system ensures data integrity by skipping invalid entries and enforcing domain-level restrictions.
